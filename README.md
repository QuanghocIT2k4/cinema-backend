# 🎬 Cinema Booking System - Backend

## ⚡ Quick Start (3 bước)

```bash
# 1. Clone code
git clone https://github.com/QuanghocIT2k4/cinema-backend.git
cd cinema-backend

# 2. Start MySQL
docker-compose up -d

# 3. Chạy backend
.\mvnw.cmd spring-boot:run
# Hoặc: .\start-local.bat (Windows)
```

✅ Backend chạy tại: `http://localhost:8080`

---

## 📋 Yêu cầu

- Java 17+
- Docker Desktop
- Maven (hoặc dùng Maven Wrapper `mvnw.cmd`)

---
